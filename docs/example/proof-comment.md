## Proof of fix — issue #214

Before/after captured under a pinned spec (viewport 1280×800, role `—`, route `/`) on 2026-09-02.

- **Symptom (before):** Selecting a vehicle shows only its number and the words Not available. A dispatcher cannot tell when it frees up. — confirmed (machine)
- **Fixed state (after):** The panel answers when the vehicle is free, shows the run on a timeline, and names an alternative. — confirmed (machine)


Reproduction steps:
1. Click `text=Vehicle 04`
2. Wait 700 ms
3. Click `text=Vehicle 10`
4. Wait 700 ms

*Attach: `02-context.png`, `03-line-zoom.png` from `deliverable/variants/` — the marked region is the measured pixel diff. `before-after.mp4` is the full motion record if anyone wants it.*
Generated by proof-of-fix; human-reviewed before attachment.
